<h1>Listing product_attributes</h1>

<table>
  <tr>
    <th>Product sku</th>
    <th>Name</th>
    <th>Short</th>
    <th>Description</th>
    <th>Value</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_attributes.each do |product_attribute| %>
  <tr>
    <td><%= product_attribute.product_sku %></td>
    <td><%= product_attribute.name %></td>
    <td><%= product_attribute.short %></td>
    <td><%= product_attribute.description %></td>
    <td><%= product_attribute.value %></td>
    <td><%= link_to 'Show', product_attribute %></td>
    <td><%= link_to 'Edit', edit_product_attribute_path(product_attribute) %></td>
    <td><%= link_to 'Destroy', product_attribute, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product attribute', new_product_attribute_path %>

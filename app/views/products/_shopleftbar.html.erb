<div class="listL">
	<ul>
		<li><span class="on"><a>葡萄酒种类</a></span>
			<ul>
				<% values = ProductAttributeValue.where(:short => :type) %>
				<% values.each do |value|%>
				<li class="<%= checked?(value.id, 7) ? '' : 'on' %>"><a href="<%= search_path value.id, 7 %>"><%= value.value %></a></li>
				<% end %>
			</ul>
			<div class="clearit"></div>
		</li>
		<li><span class="on"><a>价 格</a></span>
			<ul>
				<% values = ProductAttributeValue.where(:short => :price) %>
	        	<% values.each do |value|%>
	          	<li class="<%= checked?(value.id, 8) ? '' : 'on' %>" ><a href="<%= search_path value.id, 8 %>"><%= value.value %></a></li>
	      		<% end %>
			</ul>
			<div class="clearit"></div>
		</li>
		<li><span class="on"><a>国 家</a></span>
			<ul>
				<% values = ProductAttributeValue.where(:short => :country) %>
	          	<% values.each do |value|%>
	          	<li class="<%= checked?(value.id, 5) ? '' : 'on' %>"><a href="<%= search_path value.id, 5 %>"><%= value.value %></a></li>
	          <% end %>
			</ul>
			<div class="clearit"></div>
		</li>
		<li><span class="on"><a>使用场合</a></span>
			<ul>
				<% values = ProductAttributeValue.where(:short => :use_for) %>
	          	<% values.each do |value|%>
	          	<li class="<%= checked?(value.id, 9) ? '' : 'on' %>"><a href="<%= search_path value.id, 9 %>"><%= value.value %></a></li>
	          <% end %>
			</ul>
			<div class="clearit"></div>
		</li>
	</ul>
</div>            

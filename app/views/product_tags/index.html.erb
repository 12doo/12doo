<h1>Listing product_tags</h1>

<table>
  <tr>
    <th>Product sku</th>
    <th>Key</th>
    <th>Value</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_tags.each do |product_tag| %>
  <tr>
    <td><%= product_tag.product_sku %></td>
    <td><%= product_tag.key %></td>
    <td><%= product_tag.value %></td>
    <td><%= link_to 'Show', product_tag %></td>
    <td><%= link_to 'Edit', edit_product_tag_path(product_tag) %></td>
    <td><%= link_to 'Destroy', product_tag, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product tag', new_product_tag_path %>

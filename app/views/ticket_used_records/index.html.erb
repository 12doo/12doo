<h1>Listing ticket_used_records</h1>

<table>
  <tr>
    <th>Ticket</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ticket_used_records.each do |ticket_used_record| %>
  <tr>
    <td><%= ticket_used_record.ticket_id %></td>
    <td><%= ticket_used_record.user_id %></td>
    <td><%= link_to 'Show', ticket_used_record %></td>
    <td><%= link_to 'Edit', edit_ticket_used_record_path(ticket_used_record) %></td>
    <td><%= link_to 'Destroy', ticket_used_record, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ticket used record', new_ticket_used_record_path %>

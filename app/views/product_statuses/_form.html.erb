<%= form_for(@product_status) do |f| %>
  <% if @product_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_status.errors.count, "error") %> prohibited this product_status from being saved:</h2>

      <ul>
      <% @product_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
